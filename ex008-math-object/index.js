// Math = built-in object that provides a collection of properties and methods

let x = 3;
let y = 2;
let z = 1;

// z = Math.round(x); O z pega o valor do x. Arredonda para cima.

// z = Math.floor(x); Arredonda para baixo

// z = Math.ceil(x); Arredonda proximo de um número inteiro

// z = Math.trunc(x); Elimina qualquer porção decimal

// z = Math.pow(y, x); Potência

// z = Math.sqrt(x); Calcula a raiz quadrada

// z = Math.log(x) Calcula o logaritmo natural

// z = Math.sin(x); Calcula o seno de um ângulo

// z = Math.cos(x); Calcula o coseno

// z = Math.tan(x); Calcula o tangente

// z = Math.abs(x); Número negativo vira positivo

// z = Math.sin(x); Número positivo vira negativo
let max = Math.max(x, y, z); // Mostra o maior valor. Temos o min também.

console.log(max);

